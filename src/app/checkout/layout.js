export const metadata = {
  title: "Checkout | Maya Herbs Wholesale",
  description: "Review and confirm a Maya Herbs wholesale order.",
  robots: {
    index: false,
    follow: false,
    noarchive: true,
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
